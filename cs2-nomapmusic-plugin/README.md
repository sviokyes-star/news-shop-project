# No Map Music

Плагин для Counter-Strike 2, который отключает встроенную музыку карт для всех игроков на сервере.

## Как это работает

Музыка на картах (в том числе кастомных и воркшоп-картах) проигрывается на стороне **клиента**, поэтому плагин заглушает её, выставляя каждому игроку нулевую громкость музыкальных каналов:

- при заходе на сервер,
- на старте каждого раунда (некоторые карты перезапускают музыку по событиям).

Заглушаются каналы: основная музыка, музыка целей, начала/конца раунда, камеры смерти, MVP, Danger Zone и меню.

## Требования

- Counter-Strike 2 сервер
- [CounterStrikeSharp](https://github.com/roflmuffin/CounterStrikeSharp)
- .NET 8.0 SDK (для компиляции)

## Установка

1. Скомпилируйте плагин:
   ```bash
   # Windows
   build.bat

   # Linux/Mac
   chmod +x build.sh
   ./build.sh
   ```

2. Скопируйте `NoMapMusicPlugin.dll` из `bin/Release/net8.0/` в:
   ```
   csgo/addons/counterstrikesharp/plugins/NoMapMusicPlugin/NoMapMusicPlugin.dll
   ```

3. Перезапустите сервер.

## Примечание

Плагин отключает музыку у игроков автоматически. Игрок при желании может вернуть музыку себе в настройках звука игры — это его личная громкость.
