# CS2 No Warmup Plugin

Плагин для автоматического отключения разминки на сервере Counter-Strike 2.

## Возможности

- 🚫 Автоматическое отключение разминки при загрузке плагина
- ⚡ Мгновенное завершение разминки при старте раунда
- 🎮 Команда для принудительного завершения разминки
- 🔧 Автоматическая настройка конваров сервера

## Команды

| Команда | Описание |
|---------|----------|
| `!endwarmup` или `css_endwarmup` | Принудительно завершить разминку |

## Установка

### Требования
- Counter-Strike 2 сервер
- [CounterStrikeSharp](https://github.com/roflmuffin/CounterStrikeSharp) (v200+)
- .NET 8.0 SDK (для компиляции)

### Шаги установки

1. **Компиляция плагина:**
   ```bash
   # Windows
   build-plugin.bat
   
   # Linux/Mac
   chmod +x build-plugin.sh
   ./build-plugin.sh
   ```

2. **Установка на сервер:**
   ```
   Скопируйте NoWarmupPlugin.dll из bin/Release/net8.0/ в:
   csgo/addons/counterstrikesharp/plugins/NoWarmupPlugin/NoWarmupPlugin.dll
   ```

3. **Перезагрузка плагина:**
   ```
   В консоли сервера: css_plugins reload NoWarmupPlugin
   ```

## Как работает

1. При загрузке плагин устанавливает:
   - `mp_warmuptime 0` - время разминки 0 секунд
   - `mp_do_warmup_period 0` - отключение разминки
   - `mp_warmup_end` - принудительное завершение текущей разминки

2. При каждом старте раунда автоматически завершает разминку

3. При старте игры повторно применяет настройки

## Конвары

Плагин автоматически управляет следующими конварами:
- `mp_warmuptime` - устанавливает в 0
- `mp_do_warmup_period` - устанавливает в 0
- `mp_warmup_end` - выполняет команду

## Разработка

Версия: 1.0.0  
Автор: poehali.dev  
Лицензия: MIT

## Поддержка

Если нашли баг или есть идеи по улучшению, создайте issue в репозитории.
